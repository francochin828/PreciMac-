{
  "name": "precimac",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check macro-engine.js && node --check data/ingredients.js && node --check script.js && node --check api/foods/search.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
