{
  "name": "ai-news-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/news.js && node --check api/scrape.js && node --check script.js",
    "test": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "dependencies": {
    "fast-xml-parser": "^5.2.5"
  },
  "engines": {
    "node": ">=20"
  }
}
